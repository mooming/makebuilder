# MakeBuilder

A C++ CMake-based meta-build system that automatically generates CMakeLists.txt files based on project directory structure.

## Overview

MakeBuilder scans your project's directory structure and automatically generates CMakeLists.txt files. Instead of manually writing build configuration for each module, you simply organize your code in directories and add minimal configuration files.

## Quick Start

1. **Build MakeBuilder:**
   ```bash
   cmake -B build -S .
   cmake --build build
   ```

2. **Run on your project:**
   ```bash
   ./Application/MakeBuild/makebuild /path/to/your/project
   ```

3. **Build your project:**
   ```bash
   cmake -B build -S .
   cmake --build build
   ```

## Configuration Files

### .project.config (Project Root)

Located in the project root directory. Defines global project settings.

| Option | Default | Description |
|--------|---------|-------------|
| `requiredCMakeVersion` | 3.12 | Minimum CMake version |
| `cxxStandard` | 17 | C++ standard version |
| `compileOptions` | -Wall -Werror | Compiler flags (non-MSVC) |
| `msvcCompileOptions` | /W3 /WX | Compiler flags (MSVC) |
| `precompileDefinitions` | (none) | Preprocessor definitions |

### .module.config (Each Module)

Located in each module directory. Defines module-specific settings.

| Option | Description |
|--------|-------------|
| `name` | Module identifier (defaults to directory name if not set) |
| `buildType` | Module type (see below) |
| `precompileDefinitions` | Module-specific definitions |

**Note:** Each module directory MUST have a `.module.config` file to be recognized as a module. Directories without this file are ignored.

### Build Types

| Type | Description |
|------|-------------|
| `None` | Not a module, but may contain valid submodules |
| `Ignored` | Skip this directory |
| `HeaderOnly` | Header files only (no compilation) |
| `Executable` | Produces an executable |
| `StaticLibrary` | Static library (.a/.lib) |
| `SharedLibrary` | Shared library (.so/.dll) |
| `ExternalLibraries` | External library directory |
| `ExternalCMakeProject` | Contains existing CMakeLists.txt (auto-detected) |

### Module Specifier Files

Place these optional files in module directories:

| File | Description |
|------|-------------|
| `include.txt` | Add directory to include paths |
| `dependencies.txt` | Module dependencies (one per line) |
| `libraries.txt` | Required libraries (one per line) |
| `frameworks.txt` | Required frameworks (macOS only, e.g., OpenGL) |

## Example Project Structure

```
myproject/
├── .project.config           # Project-level settings
├── .module.config            # Root module config (buildType = None)
├── src/
│   ├── .module.config        # buildType = Executable
│   ├── main.cpp
│   └── ...
├── lib/
│   ├── .module.config        # buildType = StaticLibrary
│   ├── include.txt           # Add to include paths
│   └── mylib.cpp
└── build/                    # Generated by cmake
```

## File Detection

MakeBuilder automatically detects source and header files:

- **Source files**: `.c`, `.cpp`
- **Header files**: `.h`, `.hpp`, `.inl`

If a module has only header files and no explicit buildType, it defaults to `HeaderOnly`.

## Requirements

- CMake 3.12 or higher
- C++ compatible compiler
- Tested on macOS, Linux, and Windows
